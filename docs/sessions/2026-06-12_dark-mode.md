# 2026-06-12 — ダークモード実装

## やったこと

- 仕様書 `docs/specs/09_dark_mode.md` を作成し、CLAUDE.md の一覧に追記
- TDD で `ThemeMenuItems` / `ThemeToggleButton`（components/features/layout/theme-toggle.tsx）を実装
- next-themes の `ThemeProvider` ラッパー（components/theme-provider.tsx）を app/layout.tsx に組み込み
- globals.css に `.dark` テーマ変数一式と、ダーク用の body 光彩グラデーションを追加
- AppHeader のユーザーメニューにテーマ選択を追加、(auth) レイアウトと households ページに独立トグルを配置
- vitest.setup.ts に matchMedia スタブを追加（next-themes のシステムテーマ判定用）
- E2E `e2e/theme.spec.ts` を追加（切り替え・リロード永続化・ユーザーメニュー経由）
- Base UI のラジオ項目が選択後もメニューを閉じない問題を `closeOnClick` で修正

## 決めたこと・理由

- 切り替え方式はライト/ダーク/システムの3択（next-themes 標準パターン、localStorage 永続化）
- 切り替え UI はヘッダーのユーザードロップダウンメニュー内に配置。ヘッダーのない画面（ログイン前・グループ選択）には太陽/月アイコンボタンを右上に配置
- カラーは「温かみのある家計簿」の世界観を維持し、純黒ではなく深いグリーン系ダーク背景 + 明度を上げた income/expense/chart カラー

## 次にやること

- feat/dark-mode ブランチの PR 作成・マージ

## 未解決の課題

- E2E をフル並列で実行すると household/members 系の3テストが不安定（各テストが「アクティブグループ」というユーザー単位の共有状態を奪い合うため）。直列実行では全件パス。ダークモードとは無関係の既存問題
- アカウント単位でのテーマ同期（DB 保存）は将来検討
